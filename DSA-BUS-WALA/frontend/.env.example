# Backend API URL
VITE_BACKEND_URL=http://localhost:5000

# VAPID public key (must match backend's VAPID_PUBLIC_KEY)
VITE_VAPID_PUBLIC_KEY=

# Minimum socket update interval in ms (optional)
VITE_MIN_UPDATE_INTERVAL_MS=1000

# Vite dev server port (optional)
VITE_PORT=5173
